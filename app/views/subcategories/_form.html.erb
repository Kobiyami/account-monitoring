<%= form_with(model: @subcategory, local: true) do |form| %>
  <div>
    <%= form.label :name, 'Nom de la sous-catégorie' %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.hidden_field :category_id, value: @category.id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
